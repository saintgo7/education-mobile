[
  {
    "id": "UTIL-001",
    "title": "날짜 포맷터",
    "category": "Utilities",
    "language": "typescript",
    "framework": "Node.js",
    "code": "const formatDate = (date, format = 'YYYY-MM-DD') => { const d = new Date(date); const year = d.getFullYear(); const month = String(d.getMonth() + 1).padStart(2, '0'); const day = String(d.getDate()).padStart(2, '0'); return format.replace('YYYY', year).replace('MM', month).replace('DD', day); };"
  },
  {
    "id": "UTIL-002",
    "title": "배열 청킹",
    "category": "Utilities",
    "language": "typescript",
    "framework": "TypeScript",
    "code": "const chunk = <T>(arr: T[], size: number): T[][] => { const chunks: T[][] = []; for (let i = 0; i < arr.length; i += size) chunks.push(arr.slice(i, i + size)); return chunks; };"
  },
  {
    "id": "UTIL-003",
    "title": "딥 클론",
    "category": "Utilities",
    "language": "typescript",
    "framework": "Lodash",
    "code": "const deepClone = <T>(obj: T): T => { if (obj === null || typeof obj !== 'object') return obj; if (Array.isArray(obj)) return obj.map(deepClone) as any; return Object.fromEntries(Object.entries(obj).map(([k, v]) => [k, deepClone(v)])) as T; };"
  },
  {
    "id": "UTIL-004",
    "title": "UUID 생성기",
    "category": "Utilities",
    "language": "typescript",
    "framework": "Node.js",
    "code": "const uuid = () => 'xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx'.replace(/[xy]/g, c => { const r = Math.random() * 16 | 0; return (c === 'x' ? r : (r & 0x3 | 0x8)).toString(16); });"
  },
  {
    "id": "UTIL-005",
    "title": "쿼리 파서",
    "category": "Utilities",
    "language": "typescript",
    "framework": "TypeScript",
    "code": "const parseQuery = (search: string): Record<string, string> => { const params = new URLSearchParams(search); return Object.fromEntries(params.entries()); };"
  },
  {
    "id": "UTIL-006",
    "title": "로컬 스토리지 래퍼",
    "category": "Utilities",
    "language": "typescript",
    "framework": "Lodash",
    "code": "const storage = { get: <T>(key: string): T | null => { const item = localStorage.getItem(key); return item ? JSON.parse(item) : null; }, set: (key: string, value: any) => localStorage.setItem(key, JSON.stringify(value)), remove: (key: string) => localStorage.removeItem(key) };"
  },
  {
    "id": "UTIL-007",
    "title": "재시도 로직",
    "category": "Utilities",
    "language": "typescript",
    "framework": "Node.js",
    "code": "async function retry<T>(fn: () => Promise<T>, retries = 3, delay = 1000): Promise<T> { try { return await fn(); } catch (err) { if (retries === 0) throw err; await new Promise(r => setTimeout(r, delay)); return retry(fn, retries - 1, delay * 2); } }"
  },
  {
    "id": "UTIL-008",
    "title": "배열 유니크",
    "category": "Utilities",
    "language": "typescript",
    "framework": "TypeScript",
    "code": "const unique = <T>(arr: T[], key?: keyof T): T[] => { if (!key) return [...new Set(arr)]; const seen = new Set(); return arr.filter(item => { const k = item[key]; if (seen.has(k)) return false; seen.add(k); return true; }); };"
  },
  {
    "id": "UTIL-009",
    "title": "객체 병합",
    "category": "Utilities",
    "language": "typescript",
    "framework": "Lodash",
    "code": "const merge = <T extends object>(target: T, ...sources: Partial<T>[]): T => { return sources.reduce((acc, source) => { Object.keys(source).forEach(key => { const val = source[key]; acc[key] = val && typeof val === 'object' ? merge(acc[key] || {}, val) : val; }); return acc; }, { ...target }); };"
  },
  {
    "id": "UTIL-010",
    "title": "정규식 이스케이프",
    "category": "Utilities",
    "language": "typescript",
    "framework": "Node.js",
    "code": "const escapeRegex = (str: string): string => str.replace(/[.*+?^${}()|[\\]\\\\]/g, '\\\\$&');"
  },
  {
    "id": "UTIL-011",
    "title": "날짜 포맷터 2",
    "category": "Utilities",
    "language": "typescript",
    "framework": "TypeScript",
    "code": "const formatDate = (date, format = 'YYYY-MM-DD') => { const d = new Date(date); const year = d.getFullYear(); const month = String(d.getMonth() + 1).padStart(2, '0'); const day = String(d.getDate()).padStart(2, '0'); return format.replace('YYYY', year).replace('MM', month).replace('DD', day); };"
  },
  {
    "id": "UTIL-012",
    "title": "배열 청킹 2",
    "category": "Utilities",
    "language": "typescript",
    "framework": "Lodash",
    "code": "const chunk = <T>(arr: T[], size: number): T[][] => { const chunks: T[][] = []; for (let i = 0; i < arr.length; i += size) chunks.push(arr.slice(i, i + size)); return chunks; };"
  },
  {
    "id": "UTIL-013",
    "title": "딥 클론 2",
    "category": "Utilities",
    "language": "typescript",
    "framework": "Node.js",
    "code": "const deepClone = <T>(obj: T): T => { if (obj === null || typeof obj !== 'object') return obj; if (Array.isArray(obj)) return obj.map(deepClone) as any; return Object.fromEntries(Object.entries(obj).map(([k, v]) => [k, deepClone(v)])) as T; };"
  },
  {
    "id": "UTIL-014",
    "title": "UUID 생성기 2",
    "category": "Utilities",
    "language": "typescript",
    "framework": "TypeScript",
    "code": "const uuid = () => 'xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx'.replace(/[xy]/g, c => { const r = Math.random() * 16 | 0; return (c === 'x' ? r : (r & 0x3 | 0x8)).toString(16); });"
  },
  {
    "id": "UTIL-015",
    "title": "쿼리 파서 2",
    "category": "Utilities",
    "language": "typescript",
    "framework": "Lodash",
    "code": "const parseQuery = (search: string): Record<string, string> => { const params = new URLSearchParams(search); return Object.fromEntries(params.entries()); };"
  },
  {
    "id": "UTIL-016",
    "title": "로컬 스토리지 래퍼 2",
    "category": "Utilities",
    "language": "typescript",
    "framework": "Node.js",
    "code": "const storage = { get: <T>(key: string): T | null => { const item = localStorage.getItem(key); return item ? JSON.parse(item) : null; }, set: (key: string, value: any) => localStorage.setItem(key, JSON.stringify(value)), remove: (key: string) => localStorage.removeItem(key) };"
  },
  {
    "id": "UTIL-017",
    "title": "재시도 로직 2",
    "category": "Utilities",
    "language": "typescript",
    "framework": "TypeScript",
    "code": "async function retry<T>(fn: () => Promise<T>, retries = 3, delay = 1000): Promise<T> { try { return await fn(); } catch (err) { if (retries === 0) throw err; await new Promise(r => setTimeout(r, delay)); return retry(fn, retries - 1, delay * 2); } }"
  },
  {
    "id": "UTIL-018",
    "title": "배열 유니크 2",
    "category": "Utilities",
    "language": "typescript",
    "framework": "Lodash",
    "code": "const unique = <T>(arr: T[], key?: keyof T): T[] => { if (!key) return [...new Set(arr)]; const seen = new Set(); return arr.filter(item => { const k = item[key]; if (seen.has(k)) return false; seen.add(k); return true; }); };"
  },
  {
    "id": "UTIL-019",
    "title": "객체 병합 2",
    "category": "Utilities",
    "language": "typescript",
    "framework": "Node.js",
    "code": "const merge = <T extends object>(target: T, ...sources: Partial<T>[]): T => { return sources.reduce((acc, source) => { Object.keys(source).forEach(key => { const val = source[key]; acc[key] = val && typeof val === 'object' ? merge(acc[key] || {}, val) : val; }); return acc; }, { ...target }); };"
  },
  {
    "id": "UTIL-020",
    "title": "정규식 이스케이프 2",
    "category": "Utilities",
    "language": "typescript",
    "framework": "TypeScript",
    "code": "const escapeRegex = (str: string): string => str.replace(/[.*+?^${}()|[\\]\\\\]/g, '\\\\$&');"
  },
  {
    "id": "UTIL-021",
    "title": "날짜 포맷터 3",
    "category": "Utilities",
    "language": "typescript",
    "framework": "Lodash",
    "code": "const formatDate = (date, format = 'YYYY-MM-DD') => { const d = new Date(date); const year = d.getFullYear(); const month = String(d.getMonth() + 1).padStart(2, '0'); const day = String(d.getDate()).padStart(2, '0'); return format.replace('YYYY', year).replace('MM', month).replace('DD', day); };"
  },
  {
    "id": "UTIL-022",
    "title": "배열 청킹 3",
    "category": "Utilities",
    "language": "typescript",
    "framework": "Node.js",
    "code": "const chunk = <T>(arr: T[], size: number): T[][] => { const chunks: T[][] = []; for (let i = 0; i < arr.length; i += size) chunks.push(arr.slice(i, i + size)); return chunks; };"
  },
  {
    "id": "UTIL-023",
    "title": "딥 클론 3",
    "category": "Utilities",
    "language": "typescript",
    "framework": "TypeScript",
    "code": "const deepClone = <T>(obj: T): T => { if (obj === null || typeof obj !== 'object') return obj; if (Array.isArray(obj)) return obj.map(deepClone) as any; return Object.fromEntries(Object.entries(obj).map(([k, v]) => [k, deepClone(v)])) as T; };"
  },
  {
    "id": "UTIL-024",
    "title": "UUID 생성기 3",
    "category": "Utilities",
    "language": "typescript",
    "framework": "Lodash",
    "code": "const uuid = () => 'xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx'.replace(/[xy]/g, c => { const r = Math.random() * 16 | 0; return (c === 'x' ? r : (r & 0x3 | 0x8)).toString(16); });"
  },
  {
    "id": "UTIL-025",
    "title": "쿼리 파서 3",
    "category": "Utilities",
    "language": "typescript",
    "framework": "Node.js",
    "code": "const parseQuery = (search: string): Record<string, string> => { const params = new URLSearchParams(search); return Object.fromEntries(params.entries()); };"
  },
  {
    "id": "UTIL-026",
    "title": "로컬 스토리지 래퍼 3",
    "category": "Utilities",
    "language": "typescript",
    "framework": "TypeScript",
    "code": "const storage = { get: <T>(key: string): T | null => { const item = localStorage.getItem(key); return item ? JSON.parse(item) : null; }, set: (key: string, value: any) => localStorage.setItem(key, JSON.stringify(value)), remove: (key: string) => localStorage.removeItem(key) };"
  },
  {
    "id": "UTIL-027",
    "title": "재시도 로직 3",
    "category": "Utilities",
    "language": "typescript",
    "framework": "Lodash",
    "code": "async function retry<T>(fn: () => Promise<T>, retries = 3, delay = 1000): Promise<T> { try { return await fn(); } catch (err) { if (retries === 0) throw err; await new Promise(r => setTimeout(r, delay)); return retry(fn, retries - 1, delay * 2); } }"
  },
  {
    "id": "UTIL-028",
    "title": "배열 유니크 3",
    "category": "Utilities",
    "language": "typescript",
    "framework": "Node.js",
    "code": "const unique = <T>(arr: T[], key?: keyof T): T[] => { if (!key) return [...new Set(arr)]; const seen = new Set(); return arr.filter(item => { const k = item[key]; if (seen.has(k)) return false; seen.add(k); return true; }); };"
  },
  {
    "id": "UTIL-029",
    "title": "객체 병합 3",
    "category": "Utilities",
    "language": "typescript",
    "framework": "TypeScript",
    "code": "const merge = <T extends object>(target: T, ...sources: Partial<T>[]): T => { return sources.reduce((acc, source) => { Object.keys(source).forEach(key => { const val = source[key]; acc[key] = val && typeof val === 'object' ? merge(acc[key] || {}, val) : val; }); return acc; }, { ...target }); };"
  },
  {
    "id": "UTIL-030",
    "title": "정규식 이스케이프 3",
    "category": "Utilities",
    "language": "typescript",
    "framework": "Lodash",
    "code": "const escapeRegex = (str: string): string => str.replace(/[.*+?^${}()|[\\]\\\\]/g, '\\\\$&');"
  },
  {
    "id": "UTIL-031",
    "title": "날짜 포맷터 4",
    "category": "Utilities",
    "language": "typescript",
    "framework": "Node.js",
    "code": "const formatDate = (date, format = 'YYYY-MM-DD') => { const d = new Date(date); const year = d.getFullYear(); const month = String(d.getMonth() + 1).padStart(2, '0'); const day = String(d.getDate()).padStart(2, '0'); return format.replace('YYYY', year).replace('MM', month).replace('DD', day); };"
  },
  {
    "id": "UTIL-032",
    "title": "배열 청킹 4",
    "category": "Utilities",
    "language": "typescript",
    "framework": "TypeScript",
    "code": "const chunk = <T>(arr: T[], size: number): T[][] => { const chunks: T[][] = []; for (let i = 0; i < arr.length; i += size) chunks.push(arr.slice(i, i + size)); return chunks; };"
  },
  {
    "id": "UTIL-033",
    "title": "딥 클론 4",
    "category": "Utilities",
    "language": "typescript",
    "framework": "Lodash",
    "code": "const deepClone = <T>(obj: T): T => { if (obj === null || typeof obj !== 'object') return obj; if (Array.isArray(obj)) return obj.map(deepClone) as any; return Object.fromEntries(Object.entries(obj).map(([k, v]) => [k, deepClone(v)])) as T; };"
  },
  {
    "id": "UTIL-034",
    "title": "UUID 생성기 4",
    "category": "Utilities",
    "language": "typescript",
    "framework": "Node.js",
    "code": "const uuid = () => 'xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx'.replace(/[xy]/g, c => { const r = Math.random() * 16 | 0; return (c === 'x' ? r : (r & 0x3 | 0x8)).toString(16); });"
  },
  {
    "id": "UTIL-035",
    "title": "쿼리 파서 4",
    "category": "Utilities",
    "language": "typescript",
    "framework": "TypeScript",
    "code": "const parseQuery = (search: string): Record<string, string> => { const params = new URLSearchParams(search); return Object.fromEntries(params.entries()); };"
  },
  {
    "id": "UTIL-036",
    "title": "로컬 스토리지 래퍼 4",
    "category": "Utilities",
    "language": "typescript",
    "framework": "Lodash",
    "code": "const storage = { get: <T>(key: string): T | null => { const item = localStorage.getItem(key); return item ? JSON.parse(item) : null; }, set: (key: string, value: any) => localStorage.setItem(key, JSON.stringify(value)), remove: (key: string) => localStorage.removeItem(key) };"
  },
  {
    "id": "UTIL-037",
    "title": "재시도 로직 4",
    "category": "Utilities",
    "language": "typescript",
    "framework": "Node.js",
    "code": "async function retry<T>(fn: () => Promise<T>, retries = 3, delay = 1000): Promise<T> { try { return await fn(); } catch (err) { if (retries === 0) throw err; await new Promise(r => setTimeout(r, delay)); return retry(fn, retries - 1, delay * 2); } }"
  },
  {
    "id": "UTIL-038",
    "title": "배열 유니크 4",
    "category": "Utilities",
    "language": "typescript",
    "framework": "TypeScript",
    "code": "const unique = <T>(arr: T[], key?: keyof T): T[] => { if (!key) return [...new Set(arr)]; const seen = new Set(); return arr.filter(item => { const k = item[key]; if (seen.has(k)) return false; seen.add(k); return true; }); };"
  },
  {
    "id": "UTIL-039",
    "title": "객체 병합 4",
    "category": "Utilities",
    "language": "typescript",
    "framework": "Lodash",
    "code": "const merge = <T extends object>(target: T, ...sources: Partial<T>[]): T => { return sources.reduce((acc, source) => { Object.keys(source).forEach(key => { const val = source[key]; acc[key] = val && typeof val === 'object' ? merge(acc[key] || {}, val) : val; }); return acc; }, { ...target }); };"
  },
  {
    "id": "UTIL-040",
    "title": "정규식 이스케이프 4",
    "category": "Utilities",
    "language": "typescript",
    "framework": "Node.js",
    "code": "const escapeRegex = (str: string): string => str.replace(/[.*+?^${}()|[\\]\\\\]/g, '\\\\$&');"
  },
  {
    "id": "UTIL-041",
    "title": "날짜 포맷터 5",
    "category": "Utilities",
    "language": "typescript",
    "framework": "TypeScript",
    "code": "const formatDate = (date, format = 'YYYY-MM-DD') => { const d = new Date(date); const year = d.getFullYear(); const month = String(d.getMonth() + 1).padStart(2, '0'); const day = String(d.getDate()).padStart(2, '0'); return format.replace('YYYY', year).replace('MM', month).replace('DD', day); };"
  },
  {
    "id": "UTIL-042",
    "title": "배열 청킹 5",
    "category": "Utilities",
    "language": "typescript",
    "framework": "Lodash",
    "code": "const chunk = <T>(arr: T[], size: number): T[][] => { const chunks: T[][] = []; for (let i = 0; i < arr.length; i += size) chunks.push(arr.slice(i, i + size)); return chunks; };"
  },
  {
    "id": "UTIL-043",
    "title": "딥 클론 5",
    "category": "Utilities",
    "language": "typescript",
    "framework": "Node.js",
    "code": "const deepClone = <T>(obj: T): T => { if (obj === null || typeof obj !== 'object') return obj; if (Array.isArray(obj)) return obj.map(deepClone) as any; return Object.fromEntries(Object.entries(obj).map(([k, v]) => [k, deepClone(v)])) as T; };"
  },
  {
    "id": "UTIL-044",
    "title": "UUID 생성기 5",
    "category": "Utilities",
    "language": "typescript",
    "framework": "TypeScript",
    "code": "const uuid = () => 'xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx'.replace(/[xy]/g, c => { const r = Math.random() * 16 | 0; return (c === 'x' ? r : (r & 0x3 | 0x8)).toString(16); });"
  },
  {
    "id": "UTIL-045",
    "title": "쿼리 파서 5",
    "category": "Utilities",
    "language": "typescript",
    "framework": "Lodash",
    "code": "const parseQuery = (search: string): Record<string, string> => { const params = new URLSearchParams(search); return Object.fromEntries(params.entries()); };"
  },
  {
    "id": "UTIL-046",
    "title": "로컬 스토리지 래퍼 5",
    "category": "Utilities",
    "language": "typescript",
    "framework": "Node.js",
    "code": "const storage = { get: <T>(key: string): T | null => { const item = localStorage.getItem(key); return item ? JSON.parse(item) : null; }, set: (key: string, value: any) => localStorage.setItem(key, JSON.stringify(value)), remove: (key: string) => localStorage.removeItem(key) };"
  },
  {
    "id": "UTIL-047",
    "title": "재시도 로직 5",
    "category": "Utilities",
    "language": "typescript",
    "framework": "TypeScript",
    "code": "async function retry<T>(fn: () => Promise<T>, retries = 3, delay = 1000): Promise<T> { try { return await fn(); } catch (err) { if (retries === 0) throw err; await new Promise(r => setTimeout(r, delay)); return retry(fn, retries - 1, delay * 2); } }"
  },
  {
    "id": "UTIL-048",
    "title": "배열 유니크 5",
    "category": "Utilities",
    "language": "typescript",
    "framework": "Lodash",
    "code": "const unique = <T>(arr: T[], key?: keyof T): T[] => { if (!key) return [...new Set(arr)]; const seen = new Set(); return arr.filter(item => { const k = item[key]; if (seen.has(k)) return false; seen.add(k); return true; }); };"
  },
  {
    "id": "UTIL-049",
    "title": "객체 병합 5",
    "category": "Utilities",
    "language": "typescript",
    "framework": "Node.js",
    "code": "const merge = <T extends object>(target: T, ...sources: Partial<T>[]): T => { return sources.reduce((acc, source) => { Object.keys(source).forEach(key => { const val = source[key]; acc[key] = val && typeof val === 'object' ? merge(acc[key] || {}, val) : val; }); return acc; }, { ...target }); };"
  },
  {
    "id": "UTIL-050",
    "title": "정규식 이스케이프 5",
    "category": "Utilities",
    "language": "typescript",
    "framework": "TypeScript",
    "code": "const escapeRegex = (str: string): string => str.replace(/[.*+?^${}()|[\\]\\\\]/g, '\\\\$&');"
  },
  {
    "id": "UTIL-051",
    "title": "날짜 포맷터 6",
    "category": "Utilities",
    "language": "typescript",
    "framework": "Lodash",
    "code": "const formatDate = (date, format = 'YYYY-MM-DD') => { const d = new Date(date); const year = d.getFullYear(); const month = String(d.getMonth() + 1).padStart(2, '0'); const day = String(d.getDate()).padStart(2, '0'); return format.replace('YYYY', year).replace('MM', month).replace('DD', day); };"
  },
  {
    "id": "UTIL-052",
    "title": "배열 청킹 6",
    "category": "Utilities",
    "language": "typescript",
    "framework": "Node.js",
    "code": "const chunk = <T>(arr: T[], size: number): T[][] => { const chunks: T[][] = []; for (let i = 0; i < arr.length; i += size) chunks.push(arr.slice(i, i + size)); return chunks; };"
  },
  {
    "id": "UTIL-053",
    "title": "딥 클론 6",
    "category": "Utilities",
    "language": "typescript",
    "framework": "TypeScript",
    "code": "const deepClone = <T>(obj: T): T => { if (obj === null || typeof obj !== 'object') return obj; if (Array.isArray(obj)) return obj.map(deepClone) as any; return Object.fromEntries(Object.entries(obj).map(([k, v]) => [k, deepClone(v)])) as T; };"
  },
  {
    "id": "UTIL-054",
    "title": "UUID 생성기 6",
    "category": "Utilities",
    "language": "typescript",
    "framework": "Lodash",
    "code": "const uuid = () => 'xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx'.replace(/[xy]/g, c => { const r = Math.random() * 16 | 0; return (c === 'x' ? r : (r & 0x3 | 0x8)).toString(16); });"
  },
  {
    "id": "UTIL-055",
    "title": "쿼리 파서 6",
    "category": "Utilities",
    "language": "typescript",
    "framework": "Node.js",
    "code": "const parseQuery = (search: string): Record<string, string> => { const params = new URLSearchParams(search); return Object.fromEntries(params.entries()); };"
  },
  {
    "id": "UTIL-056",
    "title": "로컬 스토리지 래퍼 6",
    "category": "Utilities",
    "language": "typescript",
    "framework": "TypeScript",
    "code": "const storage = { get: <T>(key: string): T | null => { const item = localStorage.getItem(key); return item ? JSON.parse(item) : null; }, set: (key: string, value: any) => localStorage.setItem(key, JSON.stringify(value)), remove: (key: string) => localStorage.removeItem(key) };"
  },
  {
    "id": "UTIL-057",
    "title": "재시도 로직 6",
    "category": "Utilities",
    "language": "typescript",
    "framework": "Lodash",
    "code": "async function retry<T>(fn: () => Promise<T>, retries = 3, delay = 1000): Promise<T> { try { return await fn(); } catch (err) { if (retries === 0) throw err; await new Promise(r => setTimeout(r, delay)); return retry(fn, retries - 1, delay * 2); } }"
  },
  {
    "id": "UTIL-058",
    "title": "배열 유니크 6",
    "category": "Utilities",
    "language": "typescript",
    "framework": "Node.js",
    "code": "const unique = <T>(arr: T[], key?: keyof T): T[] => { if (!key) return [...new Set(arr)]; const seen = new Set(); return arr.filter(item => { const k = item[key]; if (seen.has(k)) return false; seen.add(k); return true; }); };"
  },
  {
    "id": "UTIL-059",
    "title": "객체 병합 6",
    "category": "Utilities",
    "language": "typescript",
    "framework": "TypeScript",
    "code": "const merge = <T extends object>(target: T, ...sources: Partial<T>[]): T => { return sources.reduce((acc, source) => { Object.keys(source).forEach(key => { const val = source[key]; acc[key] = val && typeof val === 'object' ? merge(acc[key] || {}, val) : val; }); return acc; }, { ...target }); };"
  },
  {
    "id": "UTIL-060",
    "title": "정규식 이스케이프 6",
    "category": "Utilities",
    "language": "typescript",
    "framework": "Lodash",
    "code": "const escapeRegex = (str: string): string => str.replace(/[.*+?^${}()|[\\]\\\\]/g, '\\\\$&');"
  },
  {
    "id": "UTIL-061",
    "title": "날짜 포맷터 7",
    "category": "Utilities",
    "language": "typescript",
    "framework": "Node.js",
    "code": "const formatDate = (date, format = 'YYYY-MM-DD') => { const d = new Date(date); const year = d.getFullYear(); const month = String(d.getMonth() + 1).padStart(2, '0'); const day = String(d.getDate()).padStart(2, '0'); return format.replace('YYYY', year).replace('MM', month).replace('DD', day); };"
  },
  {
    "id": "UTIL-062",
    "title": "배열 청킹 7",
    "category": "Utilities",
    "language": "typescript",
    "framework": "TypeScript",
    "code": "const chunk = <T>(arr: T[], size: number): T[][] => { const chunks: T[][] = []; for (let i = 0; i < arr.length; i += size) chunks.push(arr.slice(i, i + size)); return chunks; };"
  },
  {
    "id": "UTIL-063",
    "title": "딥 클론 7",
    "category": "Utilities",
    "language": "typescript",
    "framework": "Lodash",
    "code": "const deepClone = <T>(obj: T): T => { if (obj === null || typeof obj !== 'object') return obj; if (Array.isArray(obj)) return obj.map(deepClone) as any; return Object.fromEntries(Object.entries(obj).map(([k, v]) => [k, deepClone(v)])) as T; };"
  },
  {
    "id": "UTIL-064",
    "title": "UUID 생성기 7",
    "category": "Utilities",
    "language": "typescript",
    "framework": "Node.js",
    "code": "const uuid = () => 'xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx'.replace(/[xy]/g, c => { const r = Math.random() * 16 | 0; return (c === 'x' ? r : (r & 0x3 | 0x8)).toString(16); });"
  },
  {
    "id": "UTIL-065",
    "title": "쿼리 파서 7",
    "category": "Utilities",
    "language": "typescript",
    "framework": "TypeScript",
    "code": "const parseQuery = (search: string): Record<string, string> => { const params = new URLSearchParams(search); return Object.fromEntries(params.entries()); };"
  },
  {
    "id": "UTIL-066",
    "title": "로컬 스토리지 래퍼 7",
    "category": "Utilities",
    "language": "typescript",
    "framework": "Lodash",
    "code": "const storage = { get: <T>(key: string): T | null => { const item = localStorage.getItem(key); return item ? JSON.parse(item) : null; }, set: (key: string, value: any) => localStorage.setItem(key, JSON.stringify(value)), remove: (key: string) => localStorage.removeItem(key) };"
  },
  {
    "id": "UTIL-067",
    "title": "재시도 로직 7",
    "category": "Utilities",
    "language": "typescript",
    "framework": "Node.js",
    "code": "async function retry<T>(fn: () => Promise<T>, retries = 3, delay = 1000): Promise<T> { try { return await fn(); } catch (err) { if (retries === 0) throw err; await new Promise(r => setTimeout(r, delay)); return retry(fn, retries - 1, delay * 2); } }"
  },
  {
    "id": "UTIL-068",
    "title": "배열 유니크 7",
    "category": "Utilities",
    "language": "typescript",
    "framework": "TypeScript",
    "code": "const unique = <T>(arr: T[], key?: keyof T): T[] => { if (!key) return [...new Set(arr)]; const seen = new Set(); return arr.filter(item => { const k = item[key]; if (seen.has(k)) return false; seen.add(k); return true; }); };"
  },
  {
    "id": "UTIL-069",
    "title": "객체 병합 7",
    "category": "Utilities",
    "language": "typescript",
    "framework": "Lodash",
    "code": "const merge = <T extends object>(target: T, ...sources: Partial<T>[]): T => { return sources.reduce((acc, source) => { Object.keys(source).forEach(key => { const val = source[key]; acc[key] = val && typeof val === 'object' ? merge(acc[key] || {}, val) : val; }); return acc; }, { ...target }); };"
  },
  {
    "id": "UTIL-070",
    "title": "정규식 이스케이프 7",
    "category": "Utilities",
    "language": "typescript",
    "framework": "Node.js",
    "code": "const escapeRegex = (str: string): string => str.replace(/[.*+?^${}()|[\\]\\\\]/g, '\\\\$&');"
  },
  {
    "id": "UTIL-071",
    "title": "날짜 포맷터 8",
    "category": "Utilities",
    "language": "typescript",
    "framework": "TypeScript",
    "code": "const formatDate = (date, format = 'YYYY-MM-DD') => { const d = new Date(date); const year = d.getFullYear(); const month = String(d.getMonth() + 1).padStart(2, '0'); const day = String(d.getDate()).padStart(2, '0'); return format.replace('YYYY', year).replace('MM', month).replace('DD', day); };"
  },
  {
    "id": "UTIL-072",
    "title": "배열 청킹 8",
    "category": "Utilities",
    "language": "typescript",
    "framework": "Lodash",
    "code": "const chunk = <T>(arr: T[], size: number): T[][] => { const chunks: T[][] = []; for (let i = 0; i < arr.length; i += size) chunks.push(arr.slice(i, i + size)); return chunks; };"
  },
  {
    "id": "UTIL-073",
    "title": "딥 클론 8",
    "category": "Utilities",
    "language": "typescript",
    "framework": "Node.js",
    "code": "const deepClone = <T>(obj: T): T => { if (obj === null || typeof obj !== 'object') return obj; if (Array.isArray(obj)) return obj.map(deepClone) as any; return Object.fromEntries(Object.entries(obj).map(([k, v]) => [k, deepClone(v)])) as T; };"
  },
  {
    "id": "UTIL-074",
    "title": "UUID 생성기 8",
    "category": "Utilities",
    "language": "typescript",
    "framework": "TypeScript",
    "code": "const uuid = () => 'xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx'.replace(/[xy]/g, c => { const r = Math.random() * 16 | 0; return (c === 'x' ? r : (r & 0x3 | 0x8)).toString(16); });"
  },
  {
    "id": "UTIL-075",
    "title": "쿼리 파서 8",
    "category": "Utilities",
    "language": "typescript",
    "framework": "Lodash",
    "code": "const parseQuery = (search: string): Record<string, string> => { const params = new URLSearchParams(search); return Object.fromEntries(params.entries()); };"
  },
  {
    "id": "UTIL-076",
    "title": "로컬 스토리지 래퍼 8",
    "category": "Utilities",
    "language": "typescript",
    "framework": "Node.js",
    "code": "const storage = { get: <T>(key: string): T | null => { const item = localStorage.getItem(key); return item ? JSON.parse(item) : null; }, set: (key: string, value: any) => localStorage.setItem(key, JSON.stringify(value)), remove: (key: string) => localStorage.removeItem(key) };"
  },
  {
    "id": "UTIL-077",
    "title": "재시도 로직 8",
    "category": "Utilities",
    "language": "typescript",
    "framework": "TypeScript",
    "code": "async function retry<T>(fn: () => Promise<T>, retries = 3, delay = 1000): Promise<T> { try { return await fn(); } catch (err) { if (retries === 0) throw err; await new Promise(r => setTimeout(r, delay)); return retry(fn, retries - 1, delay * 2); } }"
  },
  {
    "id": "UTIL-078",
    "title": "배열 유니크 8",
    "category": "Utilities",
    "language": "typescript",
    "framework": "Lodash",
    "code": "const unique = <T>(arr: T[], key?: keyof T): T[] => { if (!key) return [...new Set(arr)]; const seen = new Set(); return arr.filter(item => { const k = item[key]; if (seen.has(k)) return false; seen.add(k); return true; }); };"
  },
  {
    "id": "UTIL-079",
    "title": "객체 병합 8",
    "category": "Utilities",
    "language": "typescript",
    "framework": "Node.js",
    "code": "const merge = <T extends object>(target: T, ...sources: Partial<T>[]): T => { return sources.reduce((acc, source) => { Object.keys(source).forEach(key => { const val = source[key]; acc[key] = val && typeof val === 'object' ? merge(acc[key] || {}, val) : val; }); return acc; }, { ...target }); };"
  },
  {
    "id": "UTIL-080",
    "title": "정규식 이스케이프 8",
    "category": "Utilities",
    "language": "typescript",
    "framework": "TypeScript",
    "code": "const escapeRegex = (str: string): string => str.replace(/[.*+?^${}()|[\\]\\\\]/g, '\\\\$&');"
  },
  {
    "id": "UTIL-081",
    "title": "날짜 포맷터 9",
    "category": "Utilities",
    "language": "typescript",
    "framework": "Lodash",
    "code": "const formatDate = (date, format = 'YYYY-MM-DD') => { const d = new Date(date); const year = d.getFullYear(); const month = String(d.getMonth() + 1).padStart(2, '0'); const day = String(d.getDate()).padStart(2, '0'); return format.replace('YYYY', year).replace('MM', month).replace('DD', day); };"
  },
  {
    "id": "UTIL-082",
    "title": "배열 청킹 9",
    "category": "Utilities",
    "language": "typescript",
    "framework": "Node.js",
    "code": "const chunk = <T>(arr: T[], size: number): T[][] => { const chunks: T[][] = []; for (let i = 0; i < arr.length; i += size) chunks.push(arr.slice(i, i + size)); return chunks; };"
  },
  {
    "id": "UTIL-083",
    "title": "딥 클론 9",
    "category": "Utilities",
    "language": "typescript",
    "framework": "TypeScript",
    "code": "const deepClone = <T>(obj: T): T => { if (obj === null || typeof obj !== 'object') return obj; if (Array.isArray(obj)) return obj.map(deepClone) as any; return Object.fromEntries(Object.entries(obj).map(([k, v]) => [k, deepClone(v)])) as T; };"
  },
  {
    "id": "UTIL-084",
    "title": "UUID 생성기 9",
    "category": "Utilities",
    "language": "typescript",
    "framework": "Lodash",
    "code": "const uuid = () => 'xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx'.replace(/[xy]/g, c => { const r = Math.random() * 16 | 0; return (c === 'x' ? r : (r & 0x3 | 0x8)).toString(16); });"
  },
  {
    "id": "UTIL-085",
    "title": "쿼리 파서 9",
    "category": "Utilities",
    "language": "typescript",
    "framework": "Node.js",
    "code": "const parseQuery = (search: string): Record<string, string> => { const params = new URLSearchParams(search); return Object.fromEntries(params.entries()); };"
  },
  {
    "id": "UTIL-086",
    "title": "로컬 스토리지 래퍼 9",
    "category": "Utilities",
    "language": "typescript",
    "framework": "TypeScript",
    "code": "const storage = { get: <T>(key: string): T | null => { const item = localStorage.getItem(key); return item ? JSON.parse(item) : null; }, set: (key: string, value: any) => localStorage.setItem(key, JSON.stringify(value)), remove: (key: string) => localStorage.removeItem(key) };"
  },
  {
    "id": "UTIL-087",
    "title": "재시도 로직 9",
    "category": "Utilities",
    "language": "typescript",
    "framework": "Lodash",
    "code": "async function retry<T>(fn: () => Promise<T>, retries = 3, delay = 1000): Promise<T> { try { return await fn(); } catch (err) { if (retries === 0) throw err; await new Promise(r => setTimeout(r, delay)); return retry(fn, retries - 1, delay * 2); } }"
  },
  {
    "id": "UTIL-088",
    "title": "배열 유니크 9",
    "category": "Utilities",
    "language": "typescript",
    "framework": "Node.js",
    "code": "const unique = <T>(arr: T[], key?: keyof T): T[] => { if (!key) return [...new Set(arr)]; const seen = new Set(); return arr.filter(item => { const k = item[key]; if (seen.has(k)) return false; seen.add(k); return true; }); };"
  },
  {
    "id": "UTIL-089",
    "title": "객체 병합 9",
    "category": "Utilities",
    "language": "typescript",
    "framework": "TypeScript",
    "code": "const merge = <T extends object>(target: T, ...sources: Partial<T>[]): T => { return sources.reduce((acc, source) => { Object.keys(source).forEach(key => { const val = source[key]; acc[key] = val && typeof val === 'object' ? merge(acc[key] || {}, val) : val; }); return acc; }, { ...target }); };"
  },
  {
    "id": "UTIL-090",
    "title": "정규식 이스케이프 9",
    "category": "Utilities",
    "language": "typescript",
    "framework": "Lodash",
    "code": "const escapeRegex = (str: string): string => str.replace(/[.*+?^${}()|[\\]\\\\]/g, '\\\\$&');"
  },
  {
    "id": "UTIL-091",
    "title": "날짜 포맷터 10",
    "category": "Utilities",
    "language": "typescript",
    "framework": "Node.js",
    "code": "const formatDate = (date, format = 'YYYY-MM-DD') => { const d = new Date(date); const year = d.getFullYear(); const month = String(d.getMonth() + 1).padStart(2, '0'); const day = String(d.getDate()).padStart(2, '0'); return format.replace('YYYY', year).replace('MM', month).replace('DD', day); };"
  },
  {
    "id": "UTIL-092",
    "title": "배열 청킹 10",
    "category": "Utilities",
    "language": "typescript",
    "framework": "TypeScript",
    "code": "const chunk = <T>(arr: T[], size: number): T[][] => { const chunks: T[][] = []; for (let i = 0; i < arr.length; i += size) chunks.push(arr.slice(i, i + size)); return chunks; };"
  },
  {
    "id": "UTIL-093",
    "title": "딥 클론 10",
    "category": "Utilities",
    "language": "typescript",
    "framework": "Lodash",
    "code": "const deepClone = <T>(obj: T): T => { if (obj === null || typeof obj !== 'object') return obj; if (Array.isArray(obj)) return obj.map(deepClone) as any; return Object.fromEntries(Object.entries(obj).map(([k, v]) => [k, deepClone(v)])) as T; };"
  },
  {
    "id": "UTIL-094",
    "title": "UUID 생성기 10",
    "category": "Utilities",
    "language": "typescript",
    "framework": "Node.js",
    "code": "const uuid = () => 'xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx'.replace(/[xy]/g, c => { const r = Math.random() * 16 | 0; return (c === 'x' ? r : (r & 0x3 | 0x8)).toString(16); });"
  },
  {
    "id": "UTIL-095",
    "title": "쿼리 파서 10",
    "category": "Utilities",
    "language": "typescript",
    "framework": "TypeScript",
    "code": "const parseQuery = (search: string): Record<string, string> => { const params = new URLSearchParams(search); return Object.fromEntries(params.entries()); };"
  },
  {
    "id": "UTIL-096",
    "title": "로컬 스토리지 래퍼 10",
    "category": "Utilities",
    "language": "typescript",
    "framework": "Lodash",
    "code": "const storage = { get: <T>(key: string): T | null => { const item = localStorage.getItem(key); return item ? JSON.parse(item) : null; }, set: (key: string, value: any) => localStorage.setItem(key, JSON.stringify(value)), remove: (key: string) => localStorage.removeItem(key) };"
  },
  {
    "id": "UTIL-097",
    "title": "재시도 로직 10",
    "category": "Utilities",
    "language": "typescript",
    "framework": "Node.js",
    "code": "async function retry<T>(fn: () => Promise<T>, retries = 3, delay = 1000): Promise<T> { try { return await fn(); } catch (err) { if (retries === 0) throw err; await new Promise(r => setTimeout(r, delay)); return retry(fn, retries - 1, delay * 2); } }"
  },
  {
    "id": "UTIL-098",
    "title": "배열 유니크 10",
    "category": "Utilities",
    "language": "typescript",
    "framework": "TypeScript",
    "code": "const unique = <T>(arr: T[], key?: keyof T): T[] => { if (!key) return [...new Set(arr)]; const seen = new Set(); return arr.filter(item => { const k = item[key]; if (seen.has(k)) return false; seen.add(k); return true; }); };"
  },
  {
    "id": "UTIL-099",
    "title": "객체 병합 10",
    "category": "Utilities",
    "language": "typescript",
    "framework": "Lodash",
    "code": "const merge = <T extends object>(target: T, ...sources: Partial<T>[]): T => { return sources.reduce((acc, source) => { Object.keys(source).forEach(key => { const val = source[key]; acc[key] = val && typeof val === 'object' ? merge(acc[key] || {}, val) : val; }); return acc; }, { ...target }); };"
  },
  {
    "id": "UTIL-100",
    "title": "정규식 이스케이프 10",
    "category": "Utilities",
    "language": "typescript",
    "framework": "Node.js",
    "code": "const escapeRegex = (str: string): string => str.replace(/[.*+?^${}()|[\\]\\\\]/g, '\\\\$&');"
  }
]